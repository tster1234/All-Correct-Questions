package zzusable;

//import java.time.Duration;
import org.openqa.selenium.WebDriver;
//import org.openqa.selenium.WebElement;
//import org.openqa.selenium.support.ui.ExpectedConditions;
//import org.openqa.selenium.support.ui.WebDriverWait;
//import org.testng.Assert;

public class Assertions {
	public void assertionOfSorting(WebDriver driver) {
//		WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(10));
 //       WebElement secondElement = wait.until(ExpectedConditions.elementToBeClickable(questionsort));
 //       Assert.assertTrue(secondElement.isDisplayed() && secondElement.isEnabled(), "The second element is not clickable.");
	}

}
